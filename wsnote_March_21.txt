**Q1:

my_first_i = rank_num * (n/p)

my_last_i = (rank_num+1)*(n/p)-1

*Note_Q1
- Divide chunk_size = n/p (n - input num ; p - core num)
- Identify by the rank of each process (because you'll be using MPI)

- You can change my_last_i = (i+1)*min(n/p , n-1) - 1 ; to ensure that is work even if n=13 ; p=3

**Q2:

non parallel: O(n)

SPMD: O(n/p) + O(log(p))

*Note_Q2
O(n/p) - This is run time to divide the problem

O(log(p)) - This is run time of SPMD algo. The log(p) is due to the fact taht mroe cores(p) means more performance


**Q3:

*Q3_note:
- The first example focuses on task parallelism
- Second example focuses on data parallelism
- Another way to differ is decomposition
	+ First one divide work by function or tasks
	+ Second one divide by domain or data sizes

*Q4:

*Q4_note:
- The for loop is for race condition
	+ Rank 0 will not know what msg came first and what came second
- Normal MPI_Send and MPI_Recv will wait for msg. For example if you recv a msg with tag n from process p then MPI_Recv will stall until that msg is sent to MPI_COMM_WORLD
	+ There is another alternative of this is iSend and iRecv that does not wait. Instead, it will send and recv instantly and recv will get any msg taht is in COMM_WORLD
	(dunno yet if the msg have to be sent by isend or not). For isend then it will send msg instantly without waiting for the other processes (because I think that send will send
	sequentially based on the core like 1->2->3->4)

NOTE: First assignment, focuses solely on the usage of MPI
